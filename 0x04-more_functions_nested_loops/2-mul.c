#include <stdio.h>
#include "main.h"
/**
 * mul - multiplies between two integers.
 * @a: a value
 * @b: b value
 * Return: returns the result of the operation
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
