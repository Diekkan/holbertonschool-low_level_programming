#include <stdio.h>

/**
 * main - main function
 * Return: 0 success
 */

int main(void)
{
	int n;
	int alpha;
	int newline = '\n';

	for (n = 48; n <= 57; n++)
		putchar(n);

	for (alpha = 97; alpha <= 102; alpha++)
		putchar(alpha);

	putchar(newline)

	return (0);
}
